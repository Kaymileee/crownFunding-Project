import React from "react";

const Checkbox = ({ name }) => {
  return (
    <label
      name={name}
      className="inline-block w-5 h-5 rounded border-strockColor"
    ></label>
  );
};

export default Checkbox;
