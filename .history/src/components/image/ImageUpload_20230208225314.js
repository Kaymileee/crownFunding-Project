import React from "react";

const ImageUpload = () => {
  return (
    <div className="block w-full border border-gray-200 border-dashed rounded-xl">
      <input type="file" onChange={() => {}} className="hidden" />
    </div>
  );
};

export default ImageUpload;
